/** Tarifa presencial usada para refrendación / renovación de certificados no formales. */
const TARIFA_REVALIDACION = 3;

const SUFIJO_DESCRIPCION_REVALIDACION = ' — Refrendación';

module.exports = {
  TARIFA_REVALIDACION,
  SUFIJO_DESCRIPCION_REVALIDACION,
};
