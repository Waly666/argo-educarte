/** Producción: rutas relativas; nginx del portal proxy /api y /uploads al backend. */
export const environment = {
  production: true,
  apiUrl: '/api',
  uploadsUrl: '/uploads',
  socketUrl: '',
};
